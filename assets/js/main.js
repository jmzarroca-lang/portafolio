/* ============================================
   ZARROCA — JavaScript global
   ============================================ */

// No hay JS global por ahora — cada página carga su propio JS si lo necesita
